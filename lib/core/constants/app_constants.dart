import 'package:shop_nike_app/app/app_config.dart';

class AppConstants {
  static const String rememberOnboardingStateKey = '${AppConfig.appPrefix}RememberOnboardingState';
  static const String favoriteProductsStateKey = '${AppConfig.appPrefix}FavoriteProducts';
  static const String cartProductsStateKey = '${AppConfig.appPrefix}CartProducts';
}
